import { LowercaseReplaceCasePipe } from './lowercase-replace-case.pipe';

describe('LowercaseReplaceCasePipe', () => {
  it('create an instance', () => {
    const pipe = new LowercaseReplaceCasePipe();
    expect(pipe).toBeTruthy();
  });
});
